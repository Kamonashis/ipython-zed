; Fold whole cells and individual outputs

(code_cell) @fold
(markdown_cell) @fold
(raw_cell) @fold

; any object containing an output_type pair is a notebook output

(object
  (pair key: (output_type_key))) @fold
